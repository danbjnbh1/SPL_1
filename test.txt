valgrind --leak-check=full --show-reachable=yes bin/simulation config_file.txt

planStatus 0 
changePlanPolicy bal 
planStatus 0 
didnt change the policy 
cases to test:

    # AddPlan -  error if:
         settlement doesnt exists
         selection policy doesnt exists

    # AddFacility - error if:
        facility already exists

    # PrintPlanStatus - error if:
        plan id doest exists

    # ChangePlanPolicy - error if:
        the previous policy is the same as the desired one
        the planID doesn’t exist

    # PrintActionsLog - doest print the current 'log' action

    # RestoreSimulation -  "No backup available" if not backup
    
